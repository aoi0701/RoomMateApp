{
  "name": "roommateapp-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test": "jest --forceExit",
    "test:watch": "jest --watch --forceExit"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"],
    "testTimeout": 30000
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^10.12.0",
    "jest": "^29.7.0"
  }
}
